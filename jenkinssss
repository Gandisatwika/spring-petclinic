pipeline {
    agent any

    stages {
        stage('git clone') {
            steps {
               git branch: 'main', url: 'https://github.com/Gandisatwika/spring-petclinic.git'
             }
         }
         stage('generate build') {
            steps {
                sh 'mvn package'
            }
        }
    }
}
